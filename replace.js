const fs = require('fs'); const path = require('path'); const d = 'app/api/caja'; const dirs = fs.readdirSync(d); dirs.forEach(dir => { const f = path.join(d, dir, 'route.ts'); if (fs.existsSync(f)) { let c = fs.readFileSync(f, 'utf8'); c = c.replace(/requireRole\\(session, \\['CAJERO'\\]\\);/g, 'requireRole(session, ['CAJERO', 'DUENO', 'ADMIN']);'.replace(/'/g, '')); fs.writeFileSync(f, c); } });
